# Copyright © 2022-2024, SAS Institute Inc., Cary, NC, USA. All Rights Reserved.
# SPDX-License-Identifier: Apache-2.0

---

- name: Get control plane IP from inventory
  ansible.builtin.shell: |
    grep -A1 '\[k8s_control_plane\]' {{ iac_inventory_dir }}/inventory | tail -1 | awk '{print $1}'
  register: cp_ip_output
  tags:
    - install
    - update
    - uninstall

- name: Set control plane host fact
  ansible.builtin.set_fact:
    cp_host: "{{ cp_ip_output.stdout }}"
  tags:
    - install
    - update
    - uninstall

# ✅ MUST include tags (important)
- name: Prepare SSH key option
  ansible.builtin.set_fact:
    ssh_key_option: "{{ ('-i ' + ansible_ssh_private_key_file) if (ansible_ssh_private_key_file is defined) else '' }}"
  tags:
    - install
    - update
    - uninstall

- name: Write metrics-server helm values to temp file
  ansible.builtin.copy:
    content: "{{ kubernetes_METRICS_SERVER_CONFIG | to_nice_yaml }}"
    dest: "/tmp/metrics-server-values.yaml"
  tags:
    - install
    - update

- name: Copy helm values to control plane
  ansible.builtin.shell: >
    scp -o StrictHostKeyChecking=no {{ ssh_key_option }}
    /tmp/metrics-server-values.yaml
    {{ ansible_user }}@{{ cp_host }}:/tmp/metrics-server-values.yaml
  tags:
    - install
    - update

- name: Deploy metrics-server
  ansible.builtin.shell: >
    ssh -o StrictHostKeyChecking=no {{ ssh_key_option }}
    {{ ansible_user }}@{{ cp_host }}
    "sudo KUBECONFIG=/etc/kubernetes/admin.conf /usr/local/bin/helm upgrade --install
    {{ kubernetes_METRICS_SERVER_NAME }}
    {{ kubernetes_METRICS_SERVER_CHART_NAME }}
    --repo {{ kubernetes_METRICS_SERVER_CHART_URL }}
    --version {{ kubernetes_METRICS_SERVER_CHART_VERSION }}
    --namespace {{ kubernetes_METRICS_SERVER_NAMESPACE }}
    --create-namespace
    -f /tmp/metrics-server-values.yaml
    --wait"
  tags:
    - install
    - update

- name: Remove metrics-server
  ansible.builtin.shell: >
    ssh -o StrictHostKeyChecking=no {{ ssh_key_option }}
    {{ ansible_user }}@{{ cp_host }}
    "sudo KUBECONFIG=/etc/kubernetes/admin.conf /usr/local/bin/helm uninstall
    {{ kubernetes_METRICS_SERVER_NAME }}
    --namespace kube-system --wait || true"
  tags:
    - uninstall
cloud-user@pdcesx41386:~/viya4-iac-k8s$ vim roles/kubernetes/storage/nfs-csi-driver/tasks/main.yaml
cloud-user@pdcesx41386:~/viya4-iac-k8s$ cat roles/kubernetes/storage/nfs-csi-driver/tasks/main.yaml
# Copyright © 2022-2024, SAS Institute Inc., Cary, NC, USA. All Rights Reserved.
# SPDX-License-Identifier: Apache-2.0

---

- name: Get control plane IP from inventory
  ansible.builtin.shell: |
    grep -A1 '\[k8s_control_plane\]' {{ iac_inventory_dir }}/inventory | tail -1 | awk '{print $1}'
  register: cp_ip_output
  tags:
    - install
    - update
    - uninstall

- name: Set control plane host fact
  ansible.builtin.set_fact:
    cp_host: "{{ cp_ip_output.stdout }}"
  tags:
    - install
    - update
    - uninstall

# ✅ define SSH option once for reuse
- name: Prepare SSH key option
  ansible.builtin.set_fact:
    ssh_key_option: "{{ ('-i ' + ansible_ssh_private_key_file) if (ansible_ssh_private_key_file is defined) else '' }}"
  tags:
    - install
    - update
    - uninstall

- name: Remove deprecated nfs-subdir-external-provisioner
  ansible.builtin.shell: >
    ssh -o StrictHostKeyChecking=no {{ ssh_key_option }}
    {{ ansible_user }}@{{ cp_host }}
    "sudo KUBECONFIG=/etc/kubernetes/admin.conf /usr/local/bin/helm uninstall
    nfs-subdir-external-provisioner-default
    --namespace kube-system --wait 2>/dev/null || true"
  ignore_errors: true
  tags:
    - install
    - update

- name: Write NFS CSI driver helm values to temp file
  ansible.builtin.copy:
    content: "{{ kubernetes_CSI_DRIVER_NFS_CONFIG | to_nice_yaml }}"
    dest: "/tmp/nfs-csi-values.yaml"
  tags:
    - install
    - update

- name: Copy helm values to control plane
  ansible.builtin.shell: >
    scp -o StrictHostKeyChecking=no {{ ssh_key_option }}
    /tmp/nfs-csi-values.yaml
    {{ ansible_user }}@{{ cp_host }}:/tmp/nfs-csi-values.yaml
  tags:
    - install
    - update

- name: Setting up default storage for the cluster using csi-driver-nfs
  ansible.builtin.shell: >
    ssh -o StrictHostKeyChecking=no {{ ssh_key_option }}
    {{ ansible_user }}@{{ cp_host }}
    "sudo KUBECONFIG=/etc/kubernetes/admin.conf /usr/local/bin/helm upgrade --install
    {{ kubernetes_CSI_DRIVER_NFS_NAME }}
    {{ kubernetes_CSI_DRIVER_NFS_CHART_NAME }}
    --repo {{ kubernetes_CSI_DRIVER_NFS_CHART_URL }}
    --version {{ kubernetes_CSI_DRIVER_NFS_CHART_VERSION }}
    --namespace {{ kubernetes_CSI_DRIVER_NFS_NAMESPACE }}
    --create-namespace
    -f /tmp/nfs-csi-values.yaml
    --wait"
  tags:
    - install
    - update

- name: Write NFS CSI default StorageClass to temp file
  ansible.builtin.copy:
    content: "{{ kubernetes_CSI_DRIVER_NFS_DEFAULT_SC | to_nice_yaml }}"
    dest: "/tmp/nfs-csi-default-sc.yaml"
  tags:
    - install
    - update

- name: Copy StorageClass manifest to control plane
  ansible.builtin.shell: >
    scp -o StrictHostKeyChecking=no {{ ssh_key_option }}
    /tmp/nfs-csi-default-sc.yaml
    {{ ansible_user }}@{{ cp_host }}:/tmp/nfs-csi-default-sc.yaml
  tags:
    - install
    - update

- name: Create default StorageClass for NFS CSI driver
  ansible.builtin.shell: >
    ssh -o StrictHostKeyChecking=no {{ ssh_key_option }}
    {{ ansible_user }}@{{ cp_host }}
    "sudo KUBECONFIG=/etc/kubernetes/admin.conf /usr/bin/kubectl apply -f /tmp/nfs-csi-default-sc.yaml"
  tags:
    - install
    - update

- name: Remove default StorageClass
  ansible.builtin.shell: >
    ssh -o StrictHostKeyChecking=no {{ ssh_key_option }}
    {{ ansible_user }}@{{ cp_host }}
    "sudo KUBECONFIG=/etc/kubernetes/admin.conf /usr/bin/kubectl delete storageclass default --ignore-not-found=true"
  tags:
    - uninstall

- name: Remove csi-driver-nfs
  ansible.builtin.shell: >
    ssh -o StrictHostKeyChecking=no {{ ssh_key_option }}
    {{ ansible_user }}@{{ cp_host }}
    "sudo KUBECONFIG=/etc/kubernetes/admin.conf /usr/local/bin/helm uninstall
    {{ kubernetes_CSI_DRIVER_NFS_NAME }}
    --namespace {{ kubernetes_CSI_DRIVER_NFS_NAMESPACE }} --wait || true"
  tags:
    - uninstall
cloud-user@pdcesx41386:~/viya4-iac-k8s$ cat roles/kubernetes/storage/sig-storage-local-static-provisioner/tasks/main.yaml
# Copyright © 2022-2024, SAS Institute Inc., Cary, NC, USA. All Rights Reserved.
# SPDX-License-Identifier: Apache-2.0

---
# NOTE: The usage and setting of the iac_inventory_dir variable
#       is only needed given this task is run on localhost
#       an implied localhost and this keeps the magic inventory_file
#       and inventory_dir from having values.
#
#       Reference URL : https://docs.ansible.com/ansible/latest/inventory/implicit_localhost.html
#
- name: Cleaning up old sig-storage-local-static-provisioner repo
  ansible.builtin.file:
    path: "/tmp/{{ kubernetes_LOCAL_VOLUME_NAME }}"
    state: absent
  tags:
    - install
    - update

- name: Cloning sig-storage-local-static-provisioner
  ansible.builtin.git:
    repo: "{{ kubernetes_LOCAL_VOLUME_REPO }}"
    dest: "/tmp/{{ kubernetes_LOCAL_VOLUME_NAME }}"
    version: "{{ kubernetes_LOCAL_VOLUME_REPO_VERSION }}"
  tags:
    - install
    - update

- name: Get control plane IP from inventory
  ansible.builtin.shell: |
    grep -A1 '\[k8s_control_plane\]' {{ iac_inventory_dir }}/inventory | tail -1 | awk '{print $1}'
  register: cp_ip_output
  tags:
    - install
    - update
    - uninstall

- name: Set control plane host fact
  ansible.builtin.set_fact:
    cp_host: "{{ cp_ip_output.stdout }}"
  tags:
    - install
    - update
    - uninstall
- name: Clean up old repo on control plane and copy fresh
  vars:
    ssh_key_option: "{{ ('-i ' + ansible_ssh_private_key_file) if (ansible_ssh_private_key_file is defined) else '' }}"
  ansible.builtin.shell: |
    ssh -o StrictHostKeyChecking=no {{ ssh_key_option }} \
    {{ ansible_user }}@{{ cp_host }} \
    "sudo rm -rf /tmp/{{ kubernetes_LOCAL_VOLUME_NAME }}"

    scp -r -o StrictHostKeyChecking=no {{ ssh_key_option }} \
    /tmp/{{ kubernetes_LOCAL_VOLUME_NAME }} \
    {{ ansible_user }}@{{ cp_host }}:/tmp/
  tags:
    - install
    - update

- name: Install sig-storage via helm on control plane
  vars:
    ssh_key_option: "{{ ('-i ' + ansible_ssh_private_key_file) if (ansible_ssh_private_key_file is defined) else '' }}"
  ansible.builtin.shell: >
    ssh -o StrictHostKeyChecking=no {{ ssh_key_option }}
    {{ ansible_user }}@{{ cp_host }}
    "sudo KUBECONFIG=/etc/kubernetes/admin.conf /usr/local/bin/helm upgrade --install
    {{ kubernetes_LOCAL_VOLUME_NAME }}
    /tmp/{{ kubernetes_LOCAL_VOLUME_NAME }}/helm/provisioner
    --version {{ kubernetes_LOCAL_VOLUME_CHART_VERSION }}
    --namespace {{ kubernetes_LOCAL_VOLUME_NAMESPACE }}
    --create-namespace
    --set daemonset.image.tag=v3.2.0
    --set storageClass.isDefaultClass=true
    --wait"
  tags:
    - install
    - update

- name: Remove sig-storage-local-static-provisioner
  ansible.builtin.shell: >
    ssh -o StrictHostKeyChecking=no
    -i {{ ansible_ssh_private_key_file }}
    {{ ansible_user }}@{{ cp_host }}
    "sudo KUBECONFIG=/etc/kubernetes/admin.conf /usr/local/bin/helm uninstall
    {{ kubernetes_LOCAL_VOLUME_NAME }}
    --namespace {{ kubernetes_LOCAL_VOLUME_NAMESPACE }} --wait || true"
  tags:
    - uninstall
